import { BlogPostLayout } from '@/components/BlogPostLayout'

export const post = {
  draft: false,
  author: 'Max Meldrum',
  date: '2026-03-22',
  title: "Enya - A Builder's Best Friend",
  description: 'Enya is an open-source toolkit for builders that combines observability, SQL, terminals, and code review in a single interface.',
}

export const metadata = {
    title: post.title,
    description: post.description,
    openGraph: {
      title: post.title,
      description: post.description,
      images: [{
        url: `/api/og?title=Blog&subtitle=${post.title}`,
        width: 1200,
        height: 630,
        alt: post.title,
        type: 'image/png',
      }],
      type: 'article'
    }
}

export default (props) => <BlogPostLayout article={post} {...props} />

Builders spend their days jumping between tools that were never meant to work together. Metrics, logs, traces, SQL — all in different places.

Enya is an open-source toolkit for builders, designed for both humans and AI agents. Everything a builder needs, in one place — observability, SQL, terminals, and code review. Built with those in mind that build, ship, and get paged.

<video autoPlay muted loop playsInline className="rounded-lg border border-iroh-gray-800 my-8 w-full">
  <source src="/videos/landing.mp4" type="video/mp4" />
</video>

Learn more at [enya.build](https://enya.build) — or try it directly in the browser: [enya.build/editor](https://enya.build/editor).

## Early Design Partners

We're looking for early design partners — get in touch if you want to build this with us.

Reach out at [contact@meldrumlabs.com](mailto:contact@meldrumlabs.com).
